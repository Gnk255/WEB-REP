<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Успішність групи</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>Успішність групи</h1>
    <a href="/add-student">Додати студента</a>
  </header>
  <main>
    <table>
      <thead>
        <tr>
          <th>Ім'я студента</th>
          <th>Група</th>
          <th>Предмети та оцінки</th>
          <th>Дії</th>
        </tr>
      </thead>
      <tbody>
        <% students.forEach((student, studentIndex) => { %>
          <tr>
            <td><%= student.fullName %></td>
            <td><%= student.group %></td>
            <td>
              <% student.subjects.forEach((subject, subjectIndex) => { %>
                <div>
                  <strong><%= subject.subjectName %>:</strong> <%= subject.grades.join(', ') %>
                  <form action="/delete-subject/<%= student._id %>/<%= subjectIndex %>" method="POST" style="display: inline;">
                    <button type="submit">Видалити предмет</button>
                  </form>
                </div>
              <% }) %>
            </td>
            <td>
              <a href="/add-subject/<%= student._id %>">Додати предмет/оцінку</a>
              <form action="/delete-student/<%= student._id %>" method="POST" style="display: inline;">
                <button type="submit">Видалити студента</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </main>
  <footer>
    <p>Web модуль контролю успішності здобувачів вищої освіти</p>
  </footer>
</body>
</html>
